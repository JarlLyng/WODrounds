Add your app screenshots here so they appear on the marketing site.

Required files:
- iphone.png   — iPhone app (e.g. timer screen). Suggested size: 1170×2532 px or 390×844 (point size); will be shown max 280px wide.
- watch.png    — Apple Watch app. Suggested size: 368×448 px or similar; will be shown max 184px wide.

Use PNG with transparency if you use device frames; otherwise a simple screenshot is fine. The page uses border-radius and shadow for a clean look.
